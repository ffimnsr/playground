# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Order {
  id: ID!
  providerId: ID!
  buyerUserId: ID!
  done: Boolean!
}

type OrderHistory {
  id: ID!
  orderId: ID!
  providerId: ID!
  buyerUserId: ID!
  done: Boolean!
  user: User!
}
