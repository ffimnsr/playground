# Block the fire

Make sure the person get's out of the building, by blocking the fire with walls. The exit is on the bottom right of the grid.

Represent the grid as a `Vec<Vec<u8>>`, where each cell can be one of the following:

- `0`: Empty space
- `#`: Wall
- `F`: Fire
- `P`: Person
